import { styled } from "@mui/system";

const headerStyles = {
  display: "flex",
  width: "100%",
  minHeight: "25%",
  backgroundColor: "red",
};

export const StyledHeader = styled("div")(headerStyles);
